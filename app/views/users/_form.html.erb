<%= form_for @user do |f| %>
    <div><%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %></div>
<br>
    <div class="actions">
        <%= f.submit 'Update' %>
    </div>
<% end %>